[package]
name = "semanticweft"
version = "0.1.0"
edition = "2021"
description = "Reference implementation of the SemanticWeft protocol — types, validation, graph traversal, and rendering."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v7"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"
regex = "1"

[dev-dependencies]
serde_json = "1"
